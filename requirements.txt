Flask==2.3.3
Werkzeug==2.3.7
python-multipart==0.0.6
requests==2.31.0

# Optional: For production deployment
gunicorn==21.2.0

# Optional: For WhatsApp integration (replace with actual library)
# neonize==1.0.0  # Replace with actual WhatsApp library
# whatsapp-web.py==0.1.0  # Alternative WhatsApp library

# Optional: For database (if needed)
# SQLAlchemy==2.0.23
# Flask-SQLAlchemy==3.0.5

# Optional: For environment variables
python-dotenv==1.0.0

# Optional: For CORS (if needed for frontend)
Flask-CORS==4.0.0

# Optional: For rate limiting
Flask-Limiter==3.5.0

# Optional: For logging
colorlog==6.7.0
